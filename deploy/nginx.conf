log_format timed '$remote_addr - $remote_user [$time_local] "$request" '
                 '$status $body_bytes_sent "$http_referer" "$http_user_agent" '
                 'rt=$request_time urt=$upstream_response_time';

limit_req_zone $binary_remote_addr zone=site_per_ip:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=admin_per_ip:10m rate=10r/m;

upstream django_app {
  server web:8000;
  keepalive 32;
}

server {
  listen 80;
  server_name schoolsearchzzychen.online www.schoolsearchzzychen.online;

  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl;
  http2 on;
  server_name schoolsearchzzychen.online www.schoolsearchzzychen.online;

  ssl_certificate /etc/letsencrypt/live/schoolsearchzzychen.online/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/schoolsearchzzychen.online/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_session_cache shared:SSL:10m;
  ssl_session_timeout 10m;

  client_max_body_size 10m;
  limit_req_status 429;
  server_tokens off;
  access_log /var/log/nginx/access.log timed;

  if ($query_string ~* "(?:allow_url_include|auto_prepend_file|php://|think\\app|pearcmd)") {
    return 444;
  }

  gzip on;
  gzip_comp_level 5;
  gzip_min_length 256;
  gzip_vary on;
  gzip_proxied any;
  gzip_types
    text/plain
    text/css
    text/xml
    application/json
    application/javascript
    application/xml
    application/xml+rss
    image/svg+xml;

  location /static/ {
    alias /staticfiles/;
    add_header Cache-Control "public, max-age=2592000, immutable";
  }

  location ~* "(^|/)(?:\.env|\.git|boaform|containers/json|index\.php|phpunit|vendor/phpunit|wp-|xmlrpc\.php)" {
    return 444;
  }

  location /admin/ {
    limit_req zone=admin_per_ip burst=20 nodelay;
    proxy_pass http://django_app;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location = /internal/metrics {
    allow 127.0.0.1;
    allow ::1;
    deny all;
    proxy_pass http://django_app;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Internal-Metrics "1";
  }

  location / {
    limit_req zone=site_per_ip burst=40 nodelay;
    proxy_pass http://django_app;
    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
